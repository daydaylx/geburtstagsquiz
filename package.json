{
  "name": "privatquiz",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.1",
  "scripts": {
    "dev": "corepack pnpm -r --parallel run dev",
    "build": "corepack pnpm -r run build",
    "typecheck": "corepack pnpm -r run typecheck",
    "question:studio": "node tools/question-studio/server.mjs",
    "test:studio": "vitest run --config tools/question-studio/vitest.config.mjs",
    "review:questions": "node tools/question-review/server.mjs",
    "audit:questions": "node scripts/audit-questions.mjs",
    "smoke:local": "node scripts/smoke-local-game.mjs",
    "test": "vitest run",
    "lint": "biome check apps packages scripts tools",
    "lint:fix": "biome check --write apps packages scripts tools",
    "format": "biome format --write apps packages scripts tools",
    "check": "biome check apps packages scripts tools",
    "validate": "corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test && corepack pnpm build && corepack pnpm audit:questions"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10.33.1"
  }
}
